Todo

Stage 1
- Add strcmp to find_file (Finish the code that locates files on disk)
- Load kernel.bin and populate kernel_sectors (same for ramdisk.tar)

Stage 2
- Beef up the mapping code in STAGE2

Kernel
- TTY
- vsprintf
- init pmm & vmm
- Map the kernel and execute (start writing the damn thing)